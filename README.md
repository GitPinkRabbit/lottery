# Usage

In Windows CMD or PowerShell:

```sh
.\lottery.exe <J/S/X> <MX/LG> filename.csv
```

in which, replace `<J/S/X>` into the specific contest type, `<MX/LG>` into the specific contest platform, and `filename.csv` is the MXOJ or Luogu contest ranklist CSV file.
